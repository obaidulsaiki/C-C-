#include <stdio.h>
#include <math.h>
int main()
{

  int N,i, sum=0;
  printf("Enter the integer value:");
  scanf("%d",&N);
  for (i=1;1<=N;++i)
  {
      sum=sum+i;

  }
    printf("Sum=%d",sum);
    return 0;
}

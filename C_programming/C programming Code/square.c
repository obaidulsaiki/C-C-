#include<stdio.h>
void main()
{
    int number, square;
    printf("Enter the number:");
    scanf("%d",&number);
    square= number*number;
    printf("the ans is %d ",number/6 && !(number%4));
}
